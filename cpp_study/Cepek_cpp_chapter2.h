#ifndef CEPEK_CPP_CHAPTER2
#define CEPEK_CPP_CHAPTER2

// It is recommended to replace C-macros with constants and inline functions
// in C++.
const int MAXDIM = 80;

inline int max(int x, int y)
{
	return (x > y ? x : y);
}

#endif
