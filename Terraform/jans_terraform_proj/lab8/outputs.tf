/*
output "all_users" {
  # arn  - amazon resource name
  # value = aws_iam_user.myusers[*].arn - this does not work for set anymore
  # value = values(aws_iam_user.myusers)[*].arn - this did not work for us
}*/
